stages:
  - build
  - push

variables:
  IMAGE_NAME: "registry.gitlab.com/<namespace>/<project>" # Replace with your actual namespace/project

build_image:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" $CI_REGISTRY --password-stdin
    - docker build -t $IMAGE_NAME:latest .
    - docker images

push_image:
  stage: push
  image: docker:latest
  services:
    - docker:dind
  script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" $CI_REGISTRY --password-stdin
    - docker push $IMAGE_NAME:latest
  needs:
    - build_image
